import { Link } from "react-router-dom";

const StartQuiz = () => {
    return (
        <Link to="/office">
            <button>Click Me!!!!</button>
        </Link>
    )
}

export default StartQuiz;